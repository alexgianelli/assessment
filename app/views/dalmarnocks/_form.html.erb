<%= form_with(model: dalmarnock, local: true) do |form| %>
  <% if dalmarnock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dalmarnock.errors.count, "error") %> prohibited this dalmarnock from being saved:</h2>

      <ul>
      <% dalmarnock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :timestamp %>
    <%= form.text_field :timestamp %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
